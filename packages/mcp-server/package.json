{
  "name": "@llm-output-guard/mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing Markdown output guard tools.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "llm-output-guard-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@llm-output-guard/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.23.0",
    "vitest": "^4.1.10"
  }
}
